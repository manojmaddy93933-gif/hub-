export const playVoice = async (text: string, voice: 'Zephyr' | 'Puck' | 'Charon' | 'Kore' | 'Fenrir' = 'Zephyr') => {
  // Permanently disabled per user request to remove customer notification AI voice.
  return;
};
